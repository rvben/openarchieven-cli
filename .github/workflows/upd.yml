name: upd dependency health

on:
  schedule:
    - cron: "41 6 * * 2"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: rvben/upd/.github/workflows/dependency-health.yml@b07cbd7163d771aa70cb8620c322c4083f44a3bf # v0.5.3
    with:
      upd-version: v0.5.2
      min-age: 7d
      max-bump: minor
      fail-on-blocked: true
